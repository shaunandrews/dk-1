# Calypso Visual Guide

A comprehensive visual guide to understanding how WordPress.com's Calypso dashboard works.

## Table of Contents

1. [Overview](#overview)
2. [Repository Structure](#repository-structure)
3. [Architecture Flow](#architecture-flow)
4. [Routing System](#routing-system)
5. [State Management](#state-management)
6. [Component Hierarchy](#component-hierarchy)
7. [Data Flow](#data-flow)
8. [Build & Development Process](#build--development-process)
9. [Key Concepts](#key-concepts)

---

## Overview

Calypso is **WordPress.com's custom dashboard** - a Single Page Application (SPA) that provides a modern interface for managing WordPress sites. It's built with React, TypeScript, and communicates with WordPress.com via REST API.

```
┌─────────────────────────────────────────────────────────────┐
│              WordPress.com Backend                            │
│  • REST API (wpcom API)                                      │
│  • Jetpack API                                               │
│  • User authentication                                       │
│  • Site management                                           │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ REST API
                        │ (JSON)
                        │
┌───────────────────────▼─────────────────────────────────────┐
│              Calypso (React SPA)                              │
│  • Client-side routing (page.js)                             │
│  • Redux state management                                    │
│  • TanStack Query (newer features)                           │
│  • Server-side rendering (SSR)                               │
└───────────────────────────────────────────────────────────────┘
```

**Key Characteristics:**
- ✅ Single Page Application (SPA)
- ✅ Server-side rendering support
- ✅ Redux for state management
- ✅ TanStack Query for newer features
- ✅ Code splitting by sections
- ✅ Isomorphic routing

---

## Repository Structure

```
calypso/
├── client/                    # Main application code
│   ├── components/           # Reusable UI components
│   ├── my-sites/             # Site management sections
│   ├── me/                   # User account sections
│   ├── reader/               # Reader/feed sections
│   ├── layout/               # App shell and layouts
│   ├── state/                # Redux state management
│   ├── controller/           # Route controllers
│   ├── lib/                  # Utilities and helpers
│   └── assets/stylesheets/   # Global styles
│
├── packages/                 # Internal shared packages
│   ├── calypso-router/       # Routing library (page.js wrapper)
│   ├── data-stores/          # Redux stores
│   └── ...
│
├── apps/                     # Standalone applications
│   ├── design-system-docs/   # Component documentation
│   └── ...
│
├── config/                   # Environment configuration
└── docs/                     # Documentation
```

### Main Application Structure (`client/`)

```
client/
├── components/               # 🎯 Reusable components (START HERE)
│   ├── button/
│   ├── card/
│   ├── forms/
│   └── ...
│
├── my-sites/                # Site management sections
│   ├── settings/
│   │   ├── index.js         # Route registration
│   │   ├── controller.js    # Route handlers
│   │   └── [feature]/       # Feature pages
│   ├── plugins/
│   ├── themes/
│   └── ...
│
├── me/                      # User account sections
│   ├── account/
│   ├── billing/
│   └── ...
│
├── state/                    # Redux state
│   ├── sites/
│   │   ├── reducer.js
│   │   ├── actions/
│   │   ├── selectors/
│   │   └── init.js          # Modularized state init
│   └── ...
│
├── controller/              # Route middleware
│   ├── index.web.js         # Client-side rendering
│   └── ...
│
└── sections.js              # Section definitions
```

---

## Architecture Flow

### Complete Request Flow

```
┌──────────────┐
│   Browser    │
│  Request     │
└──────┬───────┘
       │
       │ GET /settings/general/example.com
       │
┌──────▼──────────────────────────────────────────────────────┐
│  Server (Node.js) - Optional SSR                             │
│  • Server-side rendering                                     │
│  • Initial state hydration                                   │
│  • HTML generation                                           │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ HTML + Initial State
       │
┌──────▼──────────────────────────────────────────────────────┐
│  Client Bootstrap (boot/common.js)                          │
│  • Initialize Redux store                                    │
│  • Setup TanStack Query client                               │
│  • Restore persisted state                                  │
│  • Setup locale/i18n                                         │
│  • Register routes                                           │
│  • Start page.js router                                     │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Route Matching
       │
┌──────▼──────────────────────────────────────────────────────┐
│  page.js Router                                              │
│  • Matches URL to route pattern                             │
│  • Executes middleware chain                                │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Middleware Chain
       │
┌──────▼──────────────────────────────────────────────────────┐
│  Route Middleware (controller.js)                            │
│  1. siteSelection - Parse site from URL                     │
│  2. navigation - Generate sidebar                           │
│  3. [section] - Load section component                      │
│  4. makeLayout - Combine primary + secondary                │
│  5. clientRender - Render to DOM                            │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ React Render
       │
┌──────▼──────────────────────────────────────────────────────┐
│  Section Component                                           │
│  • Uses Redux selectors or TanStack Query                   │
│  • Renders UI                                                │
└──────────────────────────────────────────────────────────────┘
```

### Boot Process

```
┌─────────────────────────────────────────────────────────────┐
│  1. Load Polyfills                                           │
│     @automattic/calypso-polyfills                            │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Initialize Query Client (TanStack Query)                │
│     • Create queryClient                                     │
│     • Setup persistence                                      │
│     • Hydrate server state                                   │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Create Redux Store                                       │
│     • Combine reducers                                       │
│     • Apply middleware                                       │
│     • Restore from cache                                     │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Setup Middleware                                         │
│     • Context middleware                                     │
│     • OAuth token middleware                                 │
│     • Route middleware                                       │
│     • Analytics                                              │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  5. Register Routes                                           │
│     • Load sections.js                                       │
│     • Register all section routes                           │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  6. Start Router                                             │
│     page.start()                                             │
│     • Begin routing                                          │
│     • Handle initial route                                   │
└─────────────────────────────────────────────────────────────┘
```

---

## Routing System

### Route Registration Pattern

```
┌─────────────────────────────────────────────────────────────┐
│  Section Definition (sections.js)                            │
│                                                              │
│  {                                                           │
│    name: 'settings',                                         │
│    paths: [ '/settings/:site_id' ],                         │
│    module: 'my-sites/settings'                              │
│  }                                                           │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Auto-registered
       │
┌──────▼──────────────────────────────────────────────────────┐
│  Section Index (my-sites/settings/index.js)                  │
│                                                              │
│  export default function() {                                │
│    page(                                                    │
│      '/settings/:site_id',                                  │
│      siteSelection,    // Middleware 1                       │
│      navigation,       // Middleware 2                       │
│      settings,         // Middleware 3 (section handler)    │
│      makeLayout,       // Middleware 4                      │
│      clientRender      // Middleware 5                       │
│    );                                                        │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

### Middleware Chain

```
User navigates to /settings/general/example.com
         │
         ▼
┌────────────────────────────────────────┐
│  1. siteSelection                       │
│     • Parse :site_id from URL           │
│     • Set selected site in Redux        │
│     • context.site = site object        │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  2. navigation                          │
│     • Generate sidebar menu             │
│     • context.secondary = <Sidebar />   │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  3. settings (section handler)         │
│     • Load settings component           │
│     • Fetch data if needed             │
│     • context.primary = <Settings />   │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  4. makeLayout                          │
│     • Combine primary + secondary       │
│     • context.layout = <Layout />      │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  5. clientRender                        │
│     • Render to #wpcom                  │
│     • ReactDOM.render()                 │
└────────────────────────────────────────┘
```

### Route File Structure

```
my-sites/settings/
│
├── index.js              # Route registration
│   export default function() {
│     page('/settings/:site_id', ...);
│   }
│
├── controller.js         # Route handlers
│   export default function settings(context, next) {
│     const Settings = require('./main');
│     context.primary = <Settings siteId={siteId} />;
│   }
│
└── main.jsx              # Main component
    export default function Settings({ siteId }) {
      const site = useSelector(...);
      return <div>Settings for {site.name}</div>;
    }
```

### Section Groups

Calypso organizes routes into **section groups**:

```
┌─────────────────────────────────────────────────────────────┐
│  Section Groups                                              │
│                                                              │
│  my-sites/          # Site management                       │
│  ├── settings/     # Site settings                         │
│  ├── plugins/      # Plugin management                     │
│  ├── themes/       # Theme management                      │
│  └── ...                                                    │
│                                                              │
│  me/                # User account                           │
│  ├── account/      # Account settings                      │
│  ├── billing/      # Billing & payments                    │
│  └── ...                                                    │
│                                                              │
│  reader/            # Reader/feed                           │
│  ├── feed/         # Feed reading                          │
│  └── ...                                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## State Management

### State Architecture Evolution

```
┌─────────────────────────────────────────────────────────────┐
│  Era 1: Emitter Objects (2014-2015)                        │
│  Era 2: Facebook Flux (2015)                               │
│  Era 3: Redux Global Tree (2015-2020)                       │
│  Era 4: Modularized Redux (2020-2023)                      │
│  Era 5: TanStack Query (2023-Present) ⭐                   │
└─────────────────────────────────────────────────────────────┘
```

### Current Approach: Hybrid

```
┌─────────────────────────────────────────────────────────────┐
│  TanStack Query (New Features)                              │
│  • Hosting Dashboard                                        │
│  • Data fetching                                           │
│  • Caching & synchronization                                │
└────────────────────┬───────────────────────────────────────┘
                     │
┌────────────────────▼──────────────────────────────────────┐
│  Redux (Legacy + Modularized)                               │
│  • Sites state                                              │
│  • User state                                               │
│  • UI state                                                 │
│  • Modularized: Loaded on-demand                           │
└─────────────────────────────────────────────────────────────┘
```

### Redux State Structure

```
┌─────────────────────────────────────────────────────────────┐
│  Global State Tree                                           │
│                                                              │
│  {                                                           │
│    sites: {                                                  │
│      items: { [siteId]: site },                            │
│      selectedSiteId: 123                                    │
│    },                                                        │
│    currentUser: { ... },                                     │
│    ui: {                                                     │
│      selectedSiteId: 123,                                   │
│      masterbarVisibility: true                              │
│    },                                                        │
│    [modularized]: {                                         │
│      // Loaded on-demand                                    │
│    }                                                         │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

### Modularized State Flow

```
Component uses selector
         │
         ▼
┌────────────────────────────────────────┐
│  Import selector                        │
│  import { getSite } from 'state/sites'  │
└────────────┬───────────────────────────┘
             │
             │ Imports init.js
             ▼
┌────────────────────────────────────────┐
│  init.js (side-effect)                │
│  • Registers reducer                   │
│  • Ensures state is available          │
└────────────┬───────────────────────────┘
             │
             ▼
┌────────────────────────────────────────┐
│  Reducer loaded & registered           │
│  • State available in store              │
│  • Selector can access state            │
└────────────────────────────────────────┘
```

### TanStack Query Usage

```typescript
// In component
import { useQuery } from '@tanstack/react-query';

function MyComponent({ siteId }) {
  const { data, isLoading } = useQuery({
    queryKey: ['site', siteId],
    queryFn: () => wpcom.req.get(`/sites/${siteId}`)
  });
  
  if (isLoading) return <Spinner />;
  return <div>{data.name}</div>;
}
```

---

## Component Hierarchy

### Application Layout

```
┌─────────────────────────────────────────────────────────────┐
│  Layout Component                                            │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  Masterbar (Top Navigation)                            │ │
│  │  • Site switcher                                       │ │
│  │  • Notifications                                       │ │
│  │  • User menu                                           │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  Secondary (Sidebar)        │  Primary (Main Content) │ │
│  │  ┌──────────────────────┐   │  ┌──────────────────┐ │ │
│  │  │ Navigation Menu       │   │  │ Section Component │ │ │
│  │  │ • Settings           │   │  │                   │ │ │
│  │  │ • Plugins            │   │  │  [Page Content]   │ │ │
│  │  │ • Themes             │   │  │                   │ │ │
│  │  └──────────────────────┘   │  └──────────────────┘ │ │
│  └───────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### Component Selection Flow

```
Need a component?
         │
         ▼
┌────────────────────────────────────────┐
│  Check client/components/               │
│  (Calypso-specific components)         │
└────────────┬───────────────────────────┘
             │
             │ Not found?
             ▼
┌────────────────────────────────────────┐
│  Check @wordpress/components            │
│  (Gutenberg component library)         │
└────────────┬───────────────────────────┘
             │
             │ Still not found?
             ▼
┌────────────────────────────────────────┐
│  Create new component                   │
│  client/components/my-component/       │
└────────────────────────────────────────┘
```

---

## Data Flow

### Data Fetching Flow (Redux)

```
┌─────────────────────────────────────────────────────────────┐
│  Component                                                    │
│  const site = useSelector(getSite, siteId);                  │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Selector
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Redux Store                                                  │
│  • Check if data exists in state                            │
│  • Return cached data or undefined                          │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Data missing?
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Action Creator                                              │
│  dispatch(fetchSite(siteId));                               │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ API Request
       ▼
┌─────────────────────────────────────────────────────────────┐
│  WordPress.com REST API                                      │
│  GET /sites/{siteId}                                         │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Response
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Reducer                                                     │
│  • Receive action: SITE_RECEIVE                             │
│  • Update state.sites.items[siteId]                        │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ State Update
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Component Re-renders                                        │
│  • Selector returns new data                                │
│  • UI updates                                               │
└─────────────────────────────────────────────────────────────┘
```

### Data Fetching Flow (TanStack Query)

```
┌─────────────────────────────────────────────────────────────┐
│  Component                                                    │
│  const { data } = useQuery({                                 │
│    queryKey: ['site', siteId],                              │
│    queryFn: () => fetchSite(siteId)                         │
│  });                                                         │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Query
       ▼
┌─────────────────────────────────────────────────────────────┐
│  TanStack Query Client                                       │
│  • Check cache                                              │
│  • Return cached or fetch new                              │
│  • Handle loading/error states                              │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ API Request (if needed)
       ▼
┌─────────────────────────────────────────────────────────────┐
│  WordPress.com REST API                                      │
│  GET /sites/{siteId}                                         │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Response
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Query Client                                                │
│  • Cache response                                           │
│  • Update component                                         │
└─────────────────────────────────────────────────────────────┘
```

### State Persistence

```
┌─────────────────────────────────────────────────────────────┐
│  Redux Store Changes                                         │
│  • Action dispatched                                        │
│  • State updated                                            │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Persist Middleware
       ▼
┌─────────────────────────────────────────────────────────────┐
│  IndexedDB (Browser Storage)                                 │
│  • Store state by user ID                                    │
│  • Separate keys per state portion                          │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ On Next Load
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Restore from Cache                                          │
│  • Load persisted state                                     │
│  • Hydrate Redux store                                      │
│  • Show cached data immediately                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Build & Development Process

### Build Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Source Code                                                  │
│  • client/ sections                                         │
│  • packages/                                                │
│  • apps/                                                    │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Webpack Build
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Code Splitting                                              │
│  • Main bundle (boot code)                                 │
│  • Section chunks (lazy loaded)                            │
│  • Vendor chunks                                            │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Output
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Built Assets                                                │
│  • public/calypso.js                                        │
│  • public/[section].js (chunks)                            │
│  • public/style.css                                          │
└─────────────────────────────────────────────────────────────┘
```

### Development Workflow

```
┌─────────────────────────────────────────────────────────────┐
│  1. Start Dev Server                                         │
│     yarn start                                               │
│     • Webpack dev server                                    │
│     • Hot module replacement                                │
│     • Port: 3000                                            │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Edit Code                                                │
│     • Modify component                                      │
│     • Save file                                             │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  3. Hot Reload                                               │
│     • Webpack recompiles                                    │
│     • Browser auto-updates                                  │
│     • State preserved (HMR)                                │
└─────────────────────────────────────────────────────────────┘
```

### Code Splitting Strategy

```
┌─────────────────────────────────────────────────────────────┐
│  Entry Point                                                 │
│  client/boot/app.js                                         │
└──────┬───────────────────────────────────────────────────────┘
       │
       │ Dynamic imports
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Section Chunks (Lazy Loaded)                                │
│                                                              │
│  • my-sites/settings → settings.js                         │
│  • my-sites/plugins → plugins.js                          │
│  • me/account → account.js                                 │
│  • ...                                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## Key Concepts

### 1. Sections

Sections are the main organizational unit in Calypso:

```
┌─────────────────────────────────────────┐
│  Section = Route Group                   │
│                                          │
│  • Defined in sections.js               │
│  • Has own route patterns               │
│  • Code-split into separate chunks      │
│  • Examples:                            │
│    - my-sites/settings                  │
│    - me/account                         │
│    - reader/feed                         │
└─────────────────────────────────────────┘
```

### 2. Middleware Pattern

Routes use a middleware chain:

```
Route → Middleware 1 → Middleware 2 → ... → Render
```

Each middleware can:
- Modify `context`
- Call `next()` to continue
- Stop the chain (don't call `next()`)

### 3. Site Selection

Calypso is multi-site aware:

```
┌─────────────────────────────────────────┐
│  Site Selection Flow                     │
│                                          │
│  1. Parse site from URL                  │
│     /settings/general/example.com        │
│                                          │
│  2. Set in Redux state                   │
│     state.ui.selectedSiteId              │
│                                          │
│  3. Available throughout app            │
│     useSelector(getSelectedSiteId)       │
└─────────────────────────────────────────┘
```

### 4. Server-Side Rendering (SSR)

Calypso supports SSR for better performance:

```
┌─────────────────────────────────────────┐
│  SSR Flow                                 │
│                                          │
│  1. Server renders initial HTML          │
│  2. Hydrates Redux state                 │
│  3. Client takes over (SPA)              │
│  4. Subsequent navigation is client-side│
└─────────────────────────────────────────┘
```

### 5. Modularized State

State is loaded on-demand:

```
┌─────────────────────────────────────────┐
│  Modularized State                        │
│                                          │
│  • Reducers register lazily              │
│  • Import init.js to load state          │
│  • Reduces initial bundle size           │
│  • Better performance                    │
└─────────────────────────────────────────┘
```

### 6. Data Persistence

State is persisted to IndexedDB:

```
┌─────────────────────────────────────────┐
│  Persistence Strategy                     │
│                                          │
│  • Redux state → IndexedDB              │
│  • Separate keys per state portion       │
│  • Per-user storage                     │
│  • Restored on next load                │
│  • Shows cached data immediately         │
└─────────────────────────────────────────┘
```

---

## Quick Reference

### File Locations

| What | Where |
|------|-------|
| Components | `client/components/` |
| Sections | `client/my-sites/`, `client/me/`, `client/reader/` |
| State | `client/state/` |
| Routes | Section `index.js` files |
| Controllers | Section `controller.js` files |
| Documentation | `docs/` |

### Common Commands

```bash
# Start development server
yarn start

# Start with debug mode
yarn start:debug

# Run tests
yarn test-client

# Type checking
yarn tsc

# Lint
yarn eslint
```

### Key URLs

- **Local Dev**: http://calypso.localhost:3000
- **Production**: https://wordpress.com

### Important Imports

```typescript
// Routing
import page from '@automattic/calypso-router';

// State (Redux)
import { useSelector, useDispatch } from 'calypso/state';
import { getSite } from 'calypso/state/sites/selectors';

// State (TanStack Query)
import { useQuery } from '@tanstack/react-query';

// Components
import Button from 'calypso/components/button';
import { Button } from '@wordpress/components';

// i18n
import { useTranslate } from 'i18n-calypso';
```

---

## Next Steps

1. **Read the docs**: Check `docs/` for detailed guides
2. **Explore sections**: Look at `client/my-sites/settings/` for a complete example
3. **Check state**: See `client/state/sites/` for state management patterns
4. **Review routing**: See `docs/routing.md` for routing details

For questions, check the Calypso documentation or ask in the appropriate Slack channels.
